# Ledger de Achados de Auditoria

Fonte versionada para manter a numeracao continua dos achados. Relatorios completos ficam fora do repositorio em `C:\Codes\relatorios\repona`.

| # | Achado | Severidade | Status | Observacao |
|---|---|---|---|---|
| 1 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 2 | LWW unico mistura metadados/estoque e confia no relogio do device | Media | RESOLVIDO (rollout coordenado) | Metadados e estoque passaram a ter relogios independentes (`metadataUpdatedAt`/`inventoryUpdatedAt`) no core, Postgres e SQLite; carimbos mais de 5 minutos no futuro nao vencem o LWW. A migration Postgres 0002 e a migration mobile v10 fazem a transicao compativel, e os testes cobrem a separacao e clock skew. O deploy do schema e o novo build mobile devem ser coordenados conforme o runbook final. Resolvido no codigo em 2026-07-21. |
| 3 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 4 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 5 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 6 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 7 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 8 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 9 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 10 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 11 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 12 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. Inclui subitens 12.1, 12.2 e 12.3 citados no historico. |
| 13 | Regeneracao do token causa lockout e nao entrega a nova credencial | Alta | RESOLVIDO | casa-client.tsx agora chama setCodigo(r.novoToken) ANTES de signIn e envolve o re-login em try/catch: o novo token fica visivel na tela mesmo que o signIn rejeite (perda de rede) ou retorne error, eliminando o lockout residual apontado na revalidacao. Resolvido 2026-07-20. |
| 14 | Integridade cross-casa via FKs compostas | Variavel | RESOLVIDO | Evidencias em schema e commits anteriores. |
| 15 | Finalizacao idempotente por claim com RETURNING | Variavel | RESOLVIDO | Evidencias em modulo de listas e commits anteriores. |
| 16 | Migrations mobile versionadas por user_version | Variavel | RESOLVIDO | Evidencias em storage/database e commits anteriores. |
| 17 | Compra preserva lista de origem no sync | Variavel | RESOLVIDO | Evidencias em schema/core/historico e commits anteriores. |
| 18 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Reservado no bootstrap para manter sequencia continua. |
| 19 | Normalizacao de barcode no sync | Variavel | RESOLVIDO | Evidencias em comentarios de sync web/mobile. |
| 20 | Login web por token nao tem rate limit | Alta | RESOLVIDO | rateLimited por IP e token no authorize (options.ts). Resolvido 2026-06-26. |
| 21 | Lock distribuido de sync nao tem dono e unlock pode apagar lock alheio | Alta | RESOLVIDO | tryLock devolve token; unlock faz compare-and-delete atomico (rateLimit.ts). Resolvido 2026-06-26. |
| 22 | Claim e efeitos em round-trips separados tem resultado ambiguo em timeout | Media | RESOLVIDO | Consumo e finalizacao recebem UUID de operacao persistido no browser, executam todos os efeitos em um unico statement PostgreSQL e gravam recibo duravel em `domain_operations` somente no fim. Retry devolve o recibo sem repetir efeitos; concorrencia serializa estoque e trata a corrida de UNIQUE lendo o resultado vencedor. Teste de integracao em Postgres 16 cobre resposta perdida, duas operacoes concorrentes, replay, finalizacao unica e rollback de entrada invalida. Resolvido 2026-07-21. |
| 23 | Migrations/metadata divergem do schema atual | Alta | RESOLVIDO (CI valida no runner) | Baseline regenerado (db:generate): o 0000 defasado (redundant_magma) foi substituido por 0000_chilly_texas_twister, que reproduz o schema.ts atual (10 tabelas, colunas/indices/FKs/CHECKs, credential_version). Segundo generate confirma "No schema changes" (sem drift). Adicionado job de CI db-schema: sobe Postgres 16 efemero, aplica o baseline via psql (DDL valida) e roda drizzle-kit generate falhando se aparecer migration nova (gate anti-drift schema.ts<->baseline). README do drizzle atualizado. Fluxo canonico segue db:push. Regeneracao e gate validados localmente; a execucao do job Postgres roda no runner do CI. Resolvido 2026-07-20. |
| 24 | Web descarta brand em create/update de produto | Alta | RESOLVIDO | brand adicionado ao productInputSchema (produtos/actions.ts). Resolvido 2026-06-26. |
| 25 | Typecheck do monorepo falha por resolucao de @repona/core no mobile | Alta | RESOLVIDO | paths no tsconfig do mobile; typecheck dos 3 workspaces verde. Resolvido 2026-06-26. |
| 26 | Merge de sync persiste produtos novos antes do batch principal | Media | RESOLVIDO | O ID do produto novo e reservado pela sequence sem persistir a linha; produto, estoque, eventos, itens e contadores entram no mesmo `db.batch` transacional. Falha pode consumir apenas um numero da sequence, nunca deixar produto parcial. A cadeia de migrations e os fluxos transacionais foram validados em Postgres 16 efemero. Resolvido 2026-07-21. |
| 27 | Consumo de estoque sofre lost update em concorrencia | Media | RESOLVIDO | compare-and-set com retry no consumir (estoque.ts). Resolvido 2026-06-26. |
| 28 | Mobile deduplica eventos por nome do produto e pode duplicar apos renomeio | Media | RESOLVIDO | Dedupe por productId em sync.ts (compras/precos/consumos). Resolvido 2026-06-26. |
| 29 | Mobile aceita preco acima do limite do endpoint de sync | Media | RESOLVIDO | MAX_PRICE_CENTS no core, validado em web/mobile. Resolvido 2026-06-26. |
| 30 | Mobile pode gravar quantidade em notacao cientifica rejeitada pelo web | Media | RESOLVIDO | buildQuantityString no core usado nos modais; +2 testes. Resolvido 2026-06-26. |
| 31 | FKs de purchase_history.source_list_id tem politica de delete conflitante | Media | RESOLVIDO | A FK simples `ON DELETE SET NULL` foi removida e a FK composta passou a ser a unica politica: ela preserva o isolamento por casa e restringe delete fisico. A migration 0006 aplica o ajuste. Teste PostgreSQL prova que o delete direto e bloqueado e que um purge explicito pode nulificar `source_list_id` e excluir a lista na mesma transacao, preservando `source_list_name` e o historico. Resolvido 2026-07-21. |
| 32 | Rate limit confia diretamente em x-forwarded-for | Media | RESOLVIDO | Helper ipDaRequest prioriza x-real-ip (sync/casa/login). Resolvido 2026-06-26. |
| 33 | Validacao inicial de finalizacao considera tombstones antigos | Baixa | RESOLVIDO | Filtro deleted=false na validacao inicial (listas.ts). Resolvido 2026-06-26. |
| 34 | CSP cobre somente frame-ancestors e nao restringe scripts/estilos/conexoes | Baixa | RESOLVIDO (smoke pos-deploy) | CSP completa e ENFORCING via `src/proxy.ts`, com nonce por requisicao, `strict-dynamic`, recursos same-origin e restricoes de objeto, base, formulario e framing. O proxy tambem injeta `x-request-id`. Build e smoke local fazem parte do gate; login e Server Actions ainda devem ser confirmados no dominio publicado porque o nonce e os headers passam pela plataforma. Resolvido no codigo; validacao externa no runbook. |
| 35 | npm audit reporta vulnerabilidades moderadas em dependencias | Media | RESOLVIDO | Overrides cirurgicos e escopados atualizam apenas os transitivos vulneraveis: esbuild sob `@esbuild-kit/core-utils`, postcss sob Next/Expo Metro e uuid sob xcode. Uma instalacao limpa com `npm ci` ficou em 0 vulnerabilidades (incluindo dev), sem `audit fix --force` nem downgrade. Foram validados typecheck, testes, lint, build Next, Expo export, CLI xcode/UUID e drizzle-kit; o CI agora bloqueia qualquer advisory MODERATE ou superior. Resolvido 2026-07-21 no commit 4cd6286. |
| 36 | Script db:migrate continua exposto apesar de migration obsoleta | Media | RESOLVIDO | Script substituido por guard que falha orientando db:push (package.json). Resolvido 2026-06-26. |
| 37 | Sync nao normaliza nem protege colisao de barcode | Media | RESOLVIDO | Barcode normalizado no schema; merge preserva local em colisao (sync.ts). Resolvido 2026-06-26. |
| 38 | Scripts administrativos imprimem token da casa e usam token em nomes de backup | Baixa | RESOLVIDO | Token mascarado por padrao (flag --show-token) e casa.id nos backups. Resolvido 2026-06-26. |
| 39 | Controles mobile usam quantidade stale em acoes rapidas | Media | RESOLVIDO | changeItemQuantity usa lock por item (ref pendingItems) que serializa toques rapidos no seletor da lista, evitando incremento perdido por valor stale (App.tsx). Resolvido 2026-07-20. |
| 40 | Nova lista ativa no mobile nao propaga tombstones offline anteriores | Media | RESOLVIDO | createNewActiveShoppingList copia tombstones recentes preservando updated_at (shoppingLists.ts). Resolvido 2026-06-26. |
| 41 | Snapshot mobile pode enviar precos legados invalidos ou excessivos | Media | RESOLVIDO | buildLocalSnapshot filtra faixa valida e top 10/produto (sync.ts). Resolvido 2026-06-26. |
| 42 | Consumo web baixa estoque antes de registrar evento/status sem rollback | Media | RESOLVIDO | try/catch desfaz claim do estoque via compare-and-set (estoque.ts). Resolvido 2026-06-26. |
| 43 | Token de acesso reaparece em texto puro nas chaves persistidas de rate limit | Alta | RESOLVIDO | O codigo grava somente fingerprint HMAC-SHA-256. Em 2026-07-21, o dry-run no banco configurado encontrou 4 chaves legadas (2 login, 2 sync), o modo confirmado apagou exatamente essas 4 e a reverificacao retornou zero sem imprimir valores. O script tambem deixou de chamar `process.exit()` com requisicoes HTTP pendentes, fazendo dry-run/zero encerrarem com codigo correto no Windows. |
| 44 | Rate limit e lock distribuido falham aberto quando KV nao esta configurado | Alta | RESOLVIDO | Rate limit e lock de sync migrados para o Neon Postgres (tabelas rate_limits/sync_locks, rateLimit.ts); @vercel/kv removido; sem servico externo/custo. Resolvido 2026-06-27. |
| 45 | Exclusao de conta pela aplicacao nao e atomica | Alta | RESOLVIDO | excluirCasa usa db.batch (uma transacao neon-http) em modules/casa.ts. Resolvido 2026-06-27. |
| 46 | README orienta fluxo de migration que o projeto desativou | Media | RESOLVIDO | README aponta db:push com aviso de db:migrate desativado. Resolvido 2026-06-27. |
| 47 | Exclusao de conta e sincronizacao rate-limitam so por IP, sem chave por token | Alta | RESOLVIDO | sync/casa (DELETE) aplicam rateLimited por token (sync:token:/casa-del:token:) alem do IP. Resolvido 2026-07-01. |
| 48 | Painel /admin sem rate limit no Basic Auth | Media | RESOLVIDO | middleware.ts limita admin:ip: antes do Basic Auth; rateLimit.ts edge-safe (crypto.randomUUID global). Resolvido 2026-07-01. |
| 49 | Tabela rate_limits nunca e podada | Media | RESOLVIDO | Poda oportunistica em rateLimited (2%/hit apaga expirados >1h); sem cron. Resolvido 2026-07-01. |
| 50 | Comentario desatualizado sobre Vercel KV em sync/route.ts | Baixa | RESOLVIDO | Comentario reescrito para o backing Postgres (sem KV/memoria). Resolvido 2026-07-01. |
| 51 | Dependencia bcryptjs sem uso no codigo | Baixa | RESOLVIDO | bcryptjs e @types/bcryptjs removidos de apps/web (npm uninstall); lockfile sincronizado. Resolvido 2026-07-01. |
| 52 | Segredos reais existem em .env.local no workspace local | Alta | RESOLVIDO (controle local) | O conteudo nao foi relido. `apps/web/.env.local` continua ignorado e sem historico Git. Em 2026-07-21, a heranca e as permissoes de grupos amplos foram removidas; a ACL passou a conter somente o proprietario atual, SYSTEM e Administradores, todos por regras explicitas. O deploy continua usando o secret manager da plataforma. |
| 53 | Migracao do token para SecureStore pode deixar copia legada no SQLite | Media | RESOLVIDO | getCasaCodeSeguro tenta apagar a copia legada do SQLite toda vez que ha token seguro (best-effort, DELETE de chave ausente e no-op) em vez do early return sem limpeza (syncClient.ts). Resolvido 2026-07-20. |
| 54 | Rate limit por token usa header nao validado como chave persistida | Media | RESOLVIDO | CASA_CODE_REGEX valida x-casa-code; formato invalido cai em bucket fixo "invalido" (sync/route.ts, casa/route.ts). Resolvido 2026-07-01. |
| 55 | Sync nao e paginado e limite por Content-Length nao garante progresso | Media | RESOLVIDO | Protocolo v2 divide upload/download por colecao com keyset cursor, high-water fixo, ACK por `pageId` e sessao duravel no SecureStore. Paginas tem limites de itens e corpo lido por stream (512 KiB); retry reaplica a mesma pagina idempotente e retoma do cursor confirmado. O v1 permanece limitado a 4 MiB apenas para compatibilidade controlada. Resolvido 2026-07-21. |
| 56 | Eventos offline antigos podem nunca subir para a nuvem | Media | RESOLVIDO | O cutoff usa o menor valor entre o ultimo ACK e a janela de 24 meses; portanto todo evento criado durante um periodo offline superior a 24 meses continua elegivel. Compras historicas revividas/editadas tambem entram por `updated_at`, mesmo com `purchased_at` antigo. Primeira sync e estado invalido operam fail-open. Testes cobrem os tres casos. Resolvido 2026-07-21. |
| 57 | Falhas locais no sync mobile podem escapar sem restaurar a UI | Media | RESOLVIDO | enviarSnapshot envolve buildLocalSnapshot/json/applySnapshot em try/catch tipado; PerfilScreen usa try/finally no busy. Resolvido 2026-07-01. |
| 58 | Criacao de conta salva token antes da primeira sincronizacao completar | Media | RESOLVIDO | A operacao de criacao e o binding pendente ficam duraveis antes de prosseguir. A maquina de estados impede pareamento/criacao concorrente de substituir `pending-create`; resposta perdida repete a mesma operacao/casa, e falha da primeira sync preserva credencial, escopo de origem e retomada. Mutex de processo e testes de crash evitam casa orfa e mistura de escopo. Resolvido 2026-07-21. |
| 59 | Aplicacao de snapshot no mobile nao protege colisao de barcode | Media | RESOLVIDO | applySnapshot preserva o barcode local quando o recebido ja pertence a outro produto, espelhando o backend (sync.ts). Indice unico no SQLite fica para migracao com saneamento previo. Resolvido 2026-07-01. |
| 60 | Categoria aceita pelo sync diverge do enum validado no web | Baixa | RESOLVIDO | sync usa z.enum(CATEGORIAS).catch("Mercearia"): normaliza categoria fora do enum sem rejeitar o snapshot inteiro (sync/route.ts). Resolvido 2026-07-01. |
| 61 | Basic Auth do admin aceita credencial sem separador dois-pontos | Baixa | RESOLVIDO | middleware exige indexOf(":") != -1 antes de comparar a senha (middleware.ts). Resolvido 2026-07-01. |
| 62 | Campo mobile de alerta de estoque nao limita caracteres na UI | Baixa | RESOLVIDO | TextInput de alertThreshold usa maxLength={FIELD_LIMITS.alertThreshold} (modals.tsx). Resolvido 2026-07-01. |
| 63 | Comentario do schema de rate_limits contradiz a poda ja implementada | Baixa | RESOLVIDO | Comentario reescrito: cardinalidade nao-limitada (IP/token), limpeza e a poda oportunistica em rateLimit.ts (schema.ts). Resolvido 2026-07-01. |
| 64 | Retry de criacao/regeneracao de casa trata qualquer erro como colisao | Baixa | RESOLVIDO | ehColisaoDeCodigo restringe o retry a unique_violation (23505); demais erros propagam de imediato (casa.ts). Resolvido 2026-07-01. |
| 65 | Re-adicao de item excluido e desfeita pelo proximo sync (revive local sem un-delete no protocolo) | Media | RESOLVIDO | LWW com carimbo updated_at em purchase_history: shouldApplyIncomingDeleted no core, usado pelo merge web e applySnapshot mobile; edicoes locais carimbam. +8 testes. Requer db:push (coluna updated_at) e novo build mobile (migracao v8). Resolvido 2026-07-07. |
| 66 | Excluir compra com mais de 24 meses nunca propaga a exclusao | Media | RESOLVIDO | Tombstones de compra ignoram a janela de 24 meses no buildLocalSnapshot (sync.ts mobile). Resolvido 2026-07-07. |
| 67 | Clientes mobile antigos re-inserem como vivas as compras tombstonadas | Baixa | RESOLVIDO (retirada condicionada) | O endpoint v1 continua disponivel para clientes antigos enquanto o v2 e negociado por versao; a regra atual preserva tombstones e a telemetria registra apenas protocolo, versao, fase e resultado. A compatibilidade v1 so pode ser removida apos criterio objetivo de adocao descrito no runbook. O risco de retirada prematura foi fechado no codigo e no processo de rollout. |
| 68 | Troca de casa no mobile envia dados locais da conta anterior | Alta | RESOLVIDO (smoke em device) | Binding atomico `{code,casaId}`, arquivo SQLite por casa e `expectedCasaId` no v2 impedem envio para a conta errada. Pareamento e pull-only antes da adocao; binding legado nao verificado tambem fica pull-only. Servidor valida casa antes do merge, cursores de lista sao reescopados por casa, e create/pair/unpair/delete/restore compartilham mutex e estados duraveis de recuperacao. Testes cobrem mismatch, transicoes e limpeza apos crash; o smoke em dois aparelhos permanece evidencia de rollout. |
| 69 | Pagina publica orienta enviar a unica credencial por e-mail | Alta | RESOLVIDO | Bloco de suporte reescrito: nao pede o token e alerta para nunca envia-lo por e-mail (excluir-conta/page.tsx). Resolvido 2026-07-20. |
| 70 | Action administrativa destrutiva nao revalida autorizacao internamente | Media | RESOLVIDO | excluirCasaAction chama requireAdmin() (revalida Basic Auth por dentro); logica extraida para adminAuth.ts (Edge-safe) usada por middleware+Action. RBAC/MFA/audit-log fora de escopo (app nao tem modelo de identidade). Resolvido 2026-07-20. |
| 71 | Token bearer permanente possui apenas 40 bits de entropia | Media | RESOLVIDO (rollout coordenado) | Novos códigos usam 26 caracteres base32 (130 bits). Legados de 8/12 caracteres autenticam somente até o cutoff configurado (padrão 2027-01-01) e podem migrar até o hard end 2027-04-01; aliases nunca autenticam login/sync/delete. `PATCH /api/casa` faz rotate/migrate/recover idempotente sob advisory lock; recibos v2 exigem verifier CSPRNG do cliente e HMAC estável separado do pepper, de modo que operation ID/dump não recupera token. App 1.2 mantém estados e ACKs exatos para resposta perdida e dois aparelhos. Migration 0007, CLI agregado e testes unitários/PostgreSQL/HTTP acompanham o rollout. |
| 72 | Estoque absoluto com LWW perde consumos concorrentes entre devices | Media | RESOLVIDO (rollout coordenado) | Estoque passou a ser derivado de eventos `set` (baseline) e `consumed` (delta). UUID repetido e replay; UUIDs distintos somam consumos concorrentes independentemente da ordem de chegada. Core testa dois aparelhos partindo de 5 e consumindo 1 cada, convergindo para 3. Postgres 0002 e SQLite v10 fazem backfill com baseline; requer deploy/build coordenado. Resolvido no codigo em 2026-07-21. |
| 73 | Eventos distintos no mesmo segundo colidem na deduplicacao | Media | RESOLVIDO (rollout coordenado) | Compras, precos e eventos de estoque agora carregam `sync_id` UUID estavel, com indices UNIQUE parciais no Postgres e SQLite; identidade UUID vence a chave legada, que fica apenas como ponte para clientes antigos. Testes provam que UUIDs distintos no mesmo instante nao colidem e que replay do mesmo UUID nao duplica. Resolvido no codigo em 2026-07-21. |
| 74 | Snapshot aceito amplifica recursos e pode ultrapassar lease de 60 s | Media | RESOLVIDO | Paginas possuem tetos por colecao/corpo e quota por IP/token. Cada upload usa lease distribuida curta com fencing; o merge e os mutators web/CLI adquirem o mesmo advisory lock PostgreSQL por casa, com guard otimista para mudanca concorrente. High-water inicial e capturado sob lease+advisory, e perda de lock/concorrencia retorna erro especifico e retry seguro. Resolvido 2026-07-21. |
| 75 | API de sync aceita quantidades invalidas rejeitadas por web/mobile | Media | RESOLVIDO | canonicalQuantity agora exige match integral (^...$: numero + unidade de letras), aplica o teto MAX_QUANTITY_VALUE e positividade contextual via options.allowZero: estoque admite zero, compra/consumo/item de lista exigem positivo (mesma regra das Actions web). "1 ???", "5" sem unidade, valores acima do teto e zero em compra/consumo caem no fallback. Testes no core cobrem os casos. Resolvido 2026-07-20. |
| 76 | Normalizacao Unicode diverge entre SQLite e core de sync | Media | RESOLVIDO (rollout coordenado) | SQLite v12 repara colisões deterministicamente e o PostgreSQL passou a persistir `products.name_key`, calculado pelo mesmo `productNameKey` NFC/case-fold do core em inserts, renames, sync e CLI. O preflight/backfill fail-closed cria/preenche a coluna antes do `db:push`, detecta colisões sem imprimir domínio e instala o índice `(casa_id,name_key)`; migration 0008 protege a cadeia limpa e recusa banco povoado sem backfill. Testes reais cobrem I/İ, NFC, retry, rollback, aliases, FKs e privacidade de saída. |
| 77 | Inicializacao/migrations SQLite nao sao serializadas integralmente | Media | RESOLVIDO | initializeDatabase memoiza a inicializacao inteira (roda 1x/sessao, concorrentes compartilham a promise, zera em erro); auto-heal idempotente e best-effort recria o indice unico de nome que a migration v3 pode ter pulado (database.ts). Resolvido 2026-07-20. |
| 78 | Criacao web de produto insere produto e estoque em commits separados | Media | RESOLVIDO | createProduto virou insert unico; ausencia de inventory_items tratada por coalesce e upsert na 1a op de estoque (produtos.ts). Resolvido 2026-07-20. |
| 79 | Builds EAS de desenvolvimento/preview apontam para producao | Media | RESOLVIDO (preview local-only) | Nao existe fallback para producao: configuracao ausente falha cedo; development aponta ao emulador local e preview aponta deliberadamente para o dominio reservado `repona-preview.invalid`, tornando o build interno incapaz de tocar producao. Testes cruzam os perfis EAS e bloqueiam URL produtiva. Provisionar staging continua uma melhoria operacional antes de distribuir preview com sync, nao uma exposicao residual a producao. Resolvido 2026-07-21. |
| 80 | Perfil mobile permite criar conta antes de carregar token existente | Media | RESOLVIDO | pairedCode vira tri-state (undefined=carregando/null=nao pareado/string=pareado); enquanto undefined a UI mostra "Carregando" e nao o botao de criar conta (PerfilScreen.tsx). Resolvido 2026-07-20. |
| 81 | Decremento de quantidade fracionaria pode aumentar o valor | Media | RESOLVIDO | changeItemQuantity usa getNextInventoryQuantity+isEmptyQuantity do core (mesma regra do web): "0,8 kg" - 1 mantem o valor em vez de virar "1 kg"; helper local removido. +1 teste no core. Resolvido 2026-07-20. |
| 82 | Falha no bootstrap mobile e exibida como lista vazia | Media | RESOLVIDO | Cada flag ready (products/shopping/history) agora so vira true quando as leituras que alimentam aquela tela tiveram sucesso (productsOk/shoppingOk/historyOk), com update funcional prev||ok para nao regredir tela ja carregada num retry parcial. Uma leitura falha mantem a tela em "Carregando" + alerta de retry, nunca renderiza empty-state como sucesso. Resolvido 2026-07-20. |
| 83 | Tratamento de transporte, timeout e observabilidade e incompleto | Media | RESOLVIDO | Mobile usa timeout, valida schemas antes de tocar SQLite e produz eventos mínimos. `proxy.ts` valida/gera UUID v4 e propaga `x-request-id`; sync v1/v2 devolve o mesmo header, e o mobile preserva apenas IDs remotos seguros em falhas. Rotas/Actions registram evento, operação e código controlados sem token, payload, casaId, mensagem ou stack. Falha ao liberar lease não substitui resposta já calculada; o TTL encerra o lock e a telemetria registra `unlock_failed`. O smoke HTTP prova correlação nos dois protocolos. |
| 84 | Dados SQLite/fotos podem entrar no backup padrao do Android | Media | RESOLVIDO (AAB no rollout) | `app.json` é a fonte rastreada e define `android.allowBackup=false`; `android/` é gerado/ignorado. Além da inspeção local do manifest, a CI agora exporta o bundle, executa prebuild em cópia efêmera e falha se não existir exatamente uma `<application>` com `android:allowBackup="false"`. A inspeção do APK/AAB assinado efetivo permanece gate de publicação, documentado no runbook. |
| 85 | Scripts gravam dumps completos em JSON sem protecao explicita | Media | RESOLVIDO (risco operacional aceito) | Ambos os CLIs usam helper fail-closed: POSIX reaplica/verifica 0700/0600; Windows substitui a DACL, remove heranca e exige exatamente usuario atual, SYSTEM e Administradores com FullControl. Caminho relativo/symlink e overwrite sao rejeitados; falha de ACL remove o arquivo novo. A ACL real de `apps/web/backups` foi endurecida/verificada sem ler conteudo. O JSON continua plaintext: copia/exfiltracao, criptografia e prazo de descarte sao responsabilidade operacional explicita no runbook. |
| 86 | merge-produtos nao preserva/converge todo o estado | Media | RESOLVIDO | O CLI adquire a mesma chave `sync:lock:<casa>` do endpoint e renova/solta por token; o dry-run descreve cada vínculo e decisão. Compras (inclusive tombstones), preços e eventos são movidos sem dedupe por conteúdo: linhas legadas recebem UUID e `sync_id` é a única identidade de replay. Itens de todas as listas usam LWW com tombstone vencendo empate; estoque usa o registro mais recente, preserva os dois fluxos de eventos e grava um `set` de reconciliação. `purchase_count` considera só `deleted=false`. A migration 0003 cria `product_sync_aliases`; o merge reaponta cadeias e registra `old_sync_id -> canonical`, e o sync resolve esse alias sem permitir que metadados aposentados sobrescrevam o canônico. Plano puro e resolução de alias têm testes. Resolvido 2026-07-21. |
| 87 | Consultas e indices nao acompanham historico/rate limit em crescimento | Media | RESOLVIDO (deploy/EXPLAIN operacional) | Web pagina compras por keyset opaco em blocos de 50, filtra `purchase_history.casa_id/deleted` e consulta ultimo preco somente para os productIds da pagina. Amostras usam window function no PostgreSQL. Mobile pagina chaves/linhas, virtualiza com `SectionList` e agrega em `Map`. Indices estao no schema/migrations; `db:push` e `EXPLAIN (ANALYZE, BUFFERS)` no volume real permanecem verificacao operacional, nao lacuna de codigo. |
| 88 | Web e mobile nao possuem testes de fluxos criticos | Media | RESOLVIDO (device gate) | CI aplica toda a cadeia em PostgreSQL efêmero e roda integrações sem skip: idempotência/rollback, concorrência, lock, colisão de credencial, verifier, tenant scope, high-water, FKs, Unicode/backfill e merge. Um smoke HTTP real cobre create/replay, sync v1/v2, rotate/recover, revogação e delete/replay. Mobile exercita SQLite real, migrations, adapters de SecureStore, bindings/ACK exatos, estados de crash, paginação e cutoff. Dois aparelhos e UI visual permanecem gates do artefato, não cobertura alegada por teste unitário. |
| 89 | Variaveis criticas nao possuem schema central de ambiente | Media | RESOLVIDO | env-schema.mjs e puro (sem ler process.env) e compartilhado por runtime Next, middleware/admin, Drizzle e CLIs. DATABASE_URL exige postgres/postgresql; AUTH_SECRET/NEXTAUTH_SECRET exigem >=32 e igualdade quando ambos existem; INVITE_TOKEN_SECRET, ADMIN_SECRET e RATE_LIMIT_PEPPER definido usam o mesmo minimo; NEXTAUTH_URL exige HTTPS em producao. env:check valida o conjunto completo sem exibir valores, .env.example/README documentam o contrato e envSchema.test.ts cobre protocolos, aliases, minimos, HTTPS, pepper e erros sanitizados. Resolvido 2026-07-21. |
| 90 | POST/DELETE de conta nao sao idempotentes perante resposta perdida | Media | RESOLVIDO | Mobile persiste operation ID e, para operações que devolvem token, verifier CSPRNG no SecureStore; ACK compara exatamente ID+verifier e nunca apaga sucessor. `account_operations` v2 guarda tipo, HMAC estável do request, resultado cifrado e HMAC do verifier na mesma transação do efeito. Replay recupera o mesmo resultado, uso conflitante retorna 409 e recibos v1 de create falham fechados. DELETE v1 só é aceito se o hash legado exato ainda conferir. Testes cobrem commit sem resposta, dump/wrong verifier, concorrência e estados de crash. |
| 91 | Criacao de casa aceita disparo cross-origin sem preflight | Baixa | RESOLVIDO | POST /api/casa exige content-type application/json e valida Origin contra NEXTAUTH_URL quando presente; mobile (application/json, sem Origin) nao afetado. Resolvido 2026-07-20. |
| 92 | Controles/modais falham em semantica, foco, alvo de toque e contraste | Baixa | RESOLVIDO | Web: modais de produto/preco agora usam role=dialog, aria-modal/labelledby, foco inicial, trap de Tab/Shift+Tab, Escape e restauracao do foco; erros sao alertas, estados de filtros sao expostos e controles citados tem alvo minimo de 44 px. Mobile: botoes so-icone de produto/estoque/scanner receberam role, label e estados; modais nativos isolam a arvore acessivel, usam cabecalhos, foco inicial nos formularios e onRequestClose; alvos citados passaram a 44 px e erros dinamicos viraram alertas/live regions. Tokens ink-faint/ink3 e primary foram ajustados e testes automatizados comprovam contraste >= 4,5:1 sobre os fundos relevantes. Evidencia: typecheck web/mobile, ESLint focado e suites (web 16/16; mobile 22/22) verdes em 2026-07-21. |
| 93 | Metadados simulados aparecem como dados reais na lista mobile | Baixa | RESOLVIDO | productMeta hardcoded removido; meta derivado de categoria/quantidade reais (shoppingListPresentation.ts). Resolvido 2026-07-20. |
| 94 | Fotos persistentes ficam orfas apos edicao/exclusao | Baixa | RESOLVIDO | handleSaveProduct agora faz rollback do arquivo no catch (apaga a copia nova quando difere da URI de entrada e a escrita falhou). Adicionado collectOrphanPhotos() (products.ts) + listPersistedPhotos() (photos.ts): GC conservador que apaga arquivos do photosDir nao referenciados por nenhum products.photo_uri, rodado uma vez por sessao no startup, idempotente e best-effort. photo_uri so existe na tabela products, entao a comparacao por igualdade de URI e segura. Resolvido 2026-07-20. |
| 95 | Politica de privacidade omite Open Food Facts | Baixa | RESOLVIDO | Secao dedicada divulga a consulta ao Open Food Facts (codigo+IP, sem dados da conta) na politica. Resolvido 2026-07-20. |
| 96 | Pipeline build/CI tem fragilidades de reprodutibilidade e hardening | Baixa | RESOLVIDO NO CÓDIGO (checks remotos pendentes) | Fontes são locais, proxy/Metro seguem convenções atuais, Actions estão por SHA, Node/runner/PostgreSQL estão fixos e Gitleaks usa versão+checksum. CI valida ambiente, audit MODERATE+, lint, typecheck, testes, build, cadeia PG/anti-drift, smoke HTTP, Expo Doctor/export e manifest Android. Security executa secret scan e, quando GitHub Code Security está habilitado, CodeQL/dependency review. O SHA ainda precisa ser enviado por PR e os checks/ruleset de `main` precisam ser exigidos; nenhuma execução remota foi alegada nesta auditoria. |
| 97 | Nomes podem injetar sequencias de controle no terminal dos CLIs | Baixa | RESOLVIDO | casas.mjs e merge-produtos.mjs sanitizam C0/C1/ANSI/bidi (por code point) em todos os campos de dominio impressos. Resolvido 2026-07-20. |
| 98 | Documentacao diverge da arquitetura/configuracao atual | Baixa | RESOLVIDO | READMEs e documentos históricos distinguem arquitetura atual (Expo/SQLite → API Next → PostgreSQL) de decisões antigas; privacidade descreve nome padrão e credencial atual de 26 caracteres. O teste público cruza `CASA_CODE_LENGTH` com a página. Ledger e runbook registram migrations 0007/0008, cutoffs 2027, ordem de backfill, risco plaintext de backups e gates externos sem alegar deploy/CI/dispositivo não executado. |
| 99 | Compilacao nativa do Android falha ao resolver ponto de entrada do JS bundle em monorepo | Baixa | RESOLVIDO | Adicionado `extraPackagerArgs = ["--entry-file", "apps/mobile/index.js"]` no `react {}` em `apps/mobile/android/app/build.gradle`. O Expo CLI e Metro agora resolvem o ponto de entrada `apps/mobile/index.js` a partir da raiz do monorepo durante compilacoes nativas `assembleRelease` sem falhar a resolucao do workspace. Resolvido 2026-07-22. |
| 100 | Restauracao de backup privado requer script de ingestao e deduplicacao idempotente com name_key | Baixa | RESOLVIDO | Criados utilitarios em `apps/web/scripts/restore-backup.mjs` e `dedupe-casa.mjs`. A restauracao mapeia produtos por `name_key` NFC e insere com seguranca compras, estoque e precos historicos; a deduplicacao remove duplicatas preservando a integridade de 29 produtos e o valor total de R$ 900,28. Resolvido 2026-07-22. |


