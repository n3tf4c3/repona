# Ledger de Achados de Auditoria

Fonte versionada para manter a numeracao continua dos achados. Relatorios completos ficam fora do repositorio em `C:\Codes\relatorios\repona`.

| # | Achado | Severidade | Status | Observacao |
|---|---|---|---|---|
| 1 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 2 | LWW unico mistura metadados/estoque e confia no relogio do device | Media | ABERTO (aceito) | Parte do redesenho coordenado do protocolo de sync (revisoes/deltas por dominio atribuidas pelo servidor + limite de clock skew). Exige migracoes casadas SQLite(build EAS)+Postgres, compat com clientes 1.0.3 (ver #67) e teste multi-device; risco de corromper dados se feito mecanicamente. Requer planejamento dedicado. 2026-07-20. |
| 3 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 4 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 5 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 6 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 7 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 8 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 9 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 10 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 11 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. |
| 12 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Bootstrap a partir de comentarios e commits de auditoria anteriores. Inclui subitens 12.1, 12.2 e 12.3 citados no historico. |
| 13 | Regeneracao do token causa lockout e nao entrega a nova credencial | Alta | RESOLVIDO | casa-client.tsx agora chama setCodigo(r.novoToken) ANTES de signIn e envolve o re-login em try/catch: o novo token fica visivel na tela mesmo que o signIn rejeite (perda de rede) ou retorne error, eliminando o lockout residual apontado na revalidacao. Resolvido 2026-07-20. |
| 14 | Integridade cross-casa via FKs compostas | Variavel | RESOLVIDO | Evidencias em schema e commits anteriores. |
| 15 | Finalizacao idempotente por claim com RETURNING | Variavel | RESOLVIDO | Evidencias em modulo de listas e commits anteriores. |
| 16 | Migrations mobile versionadas por user_version | Variavel | RESOLVIDO | Evidencias em storage/database e commits anteriores. |
| 17 | Compra preserva lista de origem no sync | Variavel | RESOLVIDO | Evidencias em schema/core/historico e commits anteriores. |
| 18 | Historico anterior identificado no codigo/git | Variavel | RESOLVIDO | Reservado no bootstrap para manter sequencia continua. |
| 19 | Normalizacao de barcode no sync | Variavel | RESOLVIDO | Evidencias em comentarios de sync web/mobile. |
| 20 | Login web por token nao tem rate limit | Alta | RESOLVIDO | rateLimited por IP e token no authorize (options.ts). Resolvido 2026-06-26. |
| 21 | Lock distribuido de sync nao tem dono e unlock pode apagar lock alheio | Alta | RESOLVIDO | tryLock devolve token; unlock faz compare-and-delete atomico (rateLimit.ts). Resolvido 2026-06-26. |
| 22 | Claim e efeitos em round-trips separados tem resultado ambiguo em timeout | Media | ABERTO (aceito) | Fix exige idempotency key persistida (operation_id UNIQUE) ou funcao/CTE transacional; o driver neon-http nao tem transacao interativa. Faz parte do redesenho de idempotencia do sync (com #26, #90). Aceito nesta rodada; requer planejamento. 2026-07-20. |
| 23 | Migrations/metadata divergem do schema atual | Alta | RESOLVIDO (CI valida no runner) | Baseline regenerado (db:generate): o 0000 defasado (redundant_magma) foi substituido por 0000_chilly_texas_twister, que reproduz o schema.ts atual (10 tabelas, colunas/indices/FKs/CHECKs, credential_version). Segundo generate confirma "No schema changes" (sem drift). Adicionado job de CI db-schema: sobe Postgres 16 efemero, aplica o baseline via psql (DDL valida) e roda drizzle-kit generate falhando se aparecer migration nova (gate anti-drift schema.ts<->baseline). README do drizzle atualizado. Fluxo canonico segue db:push. Regeneracao e gate validados localmente; a execucao do job Postgres roda no runner do CI. Resolvido 2026-07-20. |
| 24 | Web descarta brand em create/update de produto | Alta | RESOLVIDO | brand adicionado ao productInputSchema (produtos/actions.ts). Resolvido 2026-06-26. |
| 25 | Typecheck do monorepo falha por resolucao de @repona/core no mobile | Alta | RESOLVIDO | paths no tsconfig do mobile; typecheck dos 3 workspaces verde. Resolvido 2026-06-26. |
| 26 | Merge de sync persiste produtos novos antes do batch principal | Media | RESOLVIDO (contencao) | Corrigida a perda de estoque: no caminho de LWW-reject o merge enfileira INSERT inventory_items ... ON CONFLICT DO NOTHING (sync.ts), preenchendo o estoque ausente quando um batch anterior falhou apos inserir o produto -- antes o empate de updatedAt fazia o continue pular o estoque para sempre (NAO era auto-curavel por retry, ao contrario do que o ledger afirmava; frase removida do comentario do merge). Eliminar o parcial de vez (transacao/CTE) segue no redesenho de idempotencia (#22/#90). Teste de integracao com Postgres pendente (#88). 2026-07-21. |
| 27 | Consumo de estoque sofre lost update em concorrencia | Media | RESOLVIDO | compare-and-set com retry no consumir (estoque.ts). Resolvido 2026-06-26. |
| 28 | Mobile deduplica eventos por nome do produto e pode duplicar apos renomeio | Media | RESOLVIDO | Dedupe por productId em sync.ts (compras/precos/consumos). Resolvido 2026-06-26. |
| 29 | Mobile aceita preco acima do limite do endpoint de sync | Media | RESOLVIDO | MAX_PRICE_CENTS no core, validado em web/mobile. Resolvido 2026-06-26. |
| 30 | Mobile pode gravar quantidade em notacao cientifica rejeitada pelo web | Media | RESOLVIDO | buildQuantityString no core usado nos modais; +2 testes. Resolvido 2026-06-26. |
| 31 | FKs de purchase_history.source_list_id tem politica de delete conflitante | Media | DOCUMENTADO | Listas continuam arquivadas, nunca deletadas isoladamente; fluxo atual nao dispara o conflito. Revalidado 2026-07-20. |
| 32 | Rate limit confia diretamente em x-forwarded-for | Media | RESOLVIDO | Helper ipDaRequest prioriza x-real-ip (sync/casa/login). Resolvido 2026-06-26. |
| 33 | Validacao inicial de finalizacao considera tombstones antigos | Baixa | RESOLVIDO | Filtro deleted=false na validacao inicial (listas.ts). Resolvido 2026-06-26. |
| 34 | CSP cobre somente frame-ancestors e nao restringe scripts/estilos/conexoes | Baixa | RESOLVIDO | CSP completa e ENFORCING via middleware (src/middleware.ts) com nonce por requisicao: default-src 'self'; script-src 'self' 'nonce-<...>' 'strict-dynamic'; style-src 'self' 'unsafe-inline' (next/font); img-src 'self' data: blob:; font-src/connect-src 'self'; object-src 'none'; base-uri/form-action 'self'; frame-ancestors 'none'; upgrade-insecure-requests. Matcher cobre todas as rotas de documento; logica admin (rate limit/DB) segue escopada a /admin. CSP estatica removida do next.config.ts. Sem recursos externos no app (tudo same-origin). Build de producao OK; decisao do proprietario de enforcar direto (risco assumido) exige smoke test em navegador pos-deploy. Resolvido 2026-07-20. |
| 35 | npm audit reporta vulnerabilidades moderadas em dependencias | Media | PARCIAL (majors agendados) | Balde 2 (patches within-range) aplicado familia por familia com validacao: tooling web (eslint 9.39.5, typescript-eslint 8.65.0, tsx 4.23.1, plugin-next 16.2.10), tailwind 4.3.3, next-auth 4.24.15, e expo 54.0.36 via expo install --fix. Este ultimo resolveu a CRITICA transitiva node-tar (via @expo/cli): 21->14 vulns, 0 criticas -> gate de CI apertado para --audit-level=critical bloqueante. Balde 3 (majors, sessao propria, alguns exigem device): Expo SDK 54->57 (+RN 0.81->0.86, agendar antes do prazo de target da Play), next-auth 5/Auth.js (fecha os 3 highs uuid), TS 5->7, eslint 10, lucide 1.x, @babel/core 8, @types/node 26. audit fix --force NAO rodado (faria downgrades: next@9, drizzle-kit@0.18). 2026-07-21. Balde 2b (2026-07-21): os 3 advisories HIGH within-range fechados via npm audit fix -- js-yaml 3.14.2->3.15.0, brace-expansion 1.1.15->1.1.16 e 5.0.6->5.0.7 (x2), shell-quote 1.8.4->1.10.0; sem --force. `npm audit --audit-level=high` verde (com e sem dev); restam 16 moderates, todos so via --force (majors do Balde 3). Gate de CI apertado critical->high (ver #96). |
| 36 | Script db:migrate continua exposto apesar de migration obsoleta | Media | RESOLVIDO | Script substituido por guard que falha orientando db:push (package.json). Resolvido 2026-06-26. |
| 37 | Sync nao normaliza nem protege colisao de barcode | Media | RESOLVIDO | Barcode normalizado no schema; merge preserva local em colisao (sync.ts). Resolvido 2026-06-26. |
| 38 | Scripts administrativos imprimem token da casa e usam token em nomes de backup | Baixa | RESOLVIDO | Token mascarado por padrao (flag --show-token) e casa.id nos backups. Resolvido 2026-06-26. |
| 39 | Controles mobile usam quantidade stale em acoes rapidas | Media | RESOLVIDO | changeItemQuantity usa lock por item (ref pendingItems) que serializa toques rapidos no seletor da lista, evitando incremento perdido por valor stale (App.tsx). Resolvido 2026-07-20. |
| 40 | Nova lista ativa no mobile nao propaga tombstones offline anteriores | Media | RESOLVIDO | createNewActiveShoppingList copia tombstones recentes preservando updated_at (shoppingLists.ts). Resolvido 2026-06-26. |
| 41 | Snapshot mobile pode enviar precos legados invalidos ou excessivos | Media | RESOLVIDO | buildLocalSnapshot filtra faixa valida e top 10/produto (sync.ts). Resolvido 2026-06-26. |
| 42 | Consumo web baixa estoque antes de registrar evento/status sem rollback | Media | RESOLVIDO | try/catch desfaz claim do estoque via compare-and-set (estoque.ts). Resolvido 2026-06-26. |
| 43 | Token de acesso reaparece em texto puro nas chaves persistidas de rate limit | Alta | RESOLVIDO (exec. operacional) | O codigo so escreve fingerprint HMAC-SHA-256 (feito antes). Adicionado scripts/limpar-rate-limit-legado.mjs (npm run rate-limit:limpar -w web): apaga deterministicamente as chaves legadas login:token:/sync:token:/casa-del:token: distinguindo-as por comprimento do sufixo (novo=43 chars base64url; legado != 43) e reverifica contagem zero, sem imprimir valores. Chaves legadas tem TTL <=24h + poda oportunistica, entao ja expiraram na pratica. Passo operacional unico: rodar `npm run rate-limit:limpar -w web -- --yes` contra prod para confirmar zero. Resolvido 2026-07-20. |
| 44 | Rate limit e lock distribuido falham aberto quando KV nao esta configurado | Alta | RESOLVIDO | Rate limit e lock de sync migrados para o Neon Postgres (tabelas rate_limits/sync_locks, rateLimit.ts); @vercel/kv removido; sem servico externo/custo. Resolvido 2026-06-27. |
| 45 | Exclusao de conta pela aplicacao nao e atomica | Alta | RESOLVIDO | excluirCasa usa db.batch (uma transacao neon-http) em modules/casa.ts. Resolvido 2026-06-27. |
| 46 | README orienta fluxo de migration que o projeto desativou | Media | RESOLVIDO | README aponta db:push com aviso de db:migrate desativado. Resolvido 2026-06-27. |
| 47 | Exclusao de conta e sincronizacao rate-limitam so por IP, sem chave por token | Alta | RESOLVIDO | sync/casa (DELETE) aplicam rateLimited por token (sync:token:/casa-del:token:) alem do IP. Resolvido 2026-07-01. |
| 48 | Painel /admin sem rate limit no Basic Auth | Media | RESOLVIDO | middleware.ts limita admin:ip: antes do Basic Auth; rateLimit.ts edge-safe (crypto.randomUUID global). Resolvido 2026-07-01. |
| 49 | Tabela rate_limits nunca e podada | Media | RESOLVIDO | Poda oportunistica em rateLimited (2%/hit apaga expirados >1h); sem cron. Resolvido 2026-07-01. |
| 50 | Comentario desatualizado sobre Vercel KV em sync/route.ts | Baixa | RESOLVIDO | Comentario reescrito para o backing Postgres (sem KV/memoria). Resolvido 2026-07-01. |
| 51 | Dependencia bcryptjs sem uso no codigo | Baixa | RESOLVIDO | bcryptjs e @types/bcryptjs removidos de apps/web (npm uninstall); lockfile sincronizado. Resolvido 2026-07-01. |
| 52 | Segredos reais existem em .env.local no workspace local | Alta | ABERTO (aceito) | Nesta auditoria somente existencia, ignore, metadata/ACL e ausencia no historico foram verificados; conteudo nao foi relido. Manter fora de backups/compartilhamentos. 2026-07-20. |
| 53 | Migracao do token para SecureStore pode deixar copia legada no SQLite | Media | RESOLVIDO | getCasaCodeSeguro tenta apagar a copia legada do SQLite toda vez que ha token seguro (best-effort, DELETE de chave ausente e no-op) em vez do early return sem limpeza (syncClient.ts). Resolvido 2026-07-20. |
| 54 | Rate limit por token usa header nao validado como chave persistida | Media | RESOLVIDO | CASA_CODE_REGEX valida x-casa-code; formato invalido cai em bucket fixo "invalido" (sync/route.ts, casa/route.ts). Resolvido 2026-07-01. |
| 55 | Sync nao e paginado e limite por Content-Length nao garante progresso | Media | ABERTO (aceito) | Fix e um protocolo incremental (cursor, chunks, ACK por pagina, erro SNAPSHOT_TOO_LARGE) — mudanca de contrato em web+mobile com compat de clientes antigos. Parte do redesenho do protocolo de sync. Aceito nesta rodada; requer planejamento. 2026-07-20. |
| 56 | Eventos offline antigos podem nunca subir para a nuvem | Media | RESOLVIDO | Primeira sincronizacao (last_sync_at nulo) envia o historico inteiro sem a janela (sync.ts). Residual: >24 meses offline ENTRE syncs continua filtrado (aceito). Resolvido 2026-07-01. |
| 57 | Falhas locais no sync mobile podem escapar sem restaurar a UI | Media | RESOLVIDO | enviarSnapshot envolve buildLocalSnapshot/json/applySnapshot em try/catch tipado; PerfilScreen usa try/finally no busy. Resolvido 2026-07-01. |
| 58 | Criacao de conta salva token antes da primeira sincronizacao completar | Media | RESOLVIDO (contencao) | criarConta agora persiste o token no SecureStore ANTES da primeira sincronizacao (syncClient.ts): a unica credencial da casa recem-criada nao se perde se a 1a sync falhar (timeout/413/crash), e a UI pareada nao reoferece "criar conta" (evita 2a casa orfa). O active_casa_id so e gravado apos a sync bem-sucedida; no arranque restoreActiveScope abre o scratch 'local' (nao a casa vazia), que a proxima syncNow empurra e adota. Residual: indicador explicito de "backup pendente" + retry automatico na UI; idempotencia do POST perante resposta perdida ANTES do token segue no #90. 2026-07-21. |
| 59 | Aplicacao de snapshot no mobile nao protege colisao de barcode | Media | RESOLVIDO | applySnapshot preserva o barcode local quando o recebido ja pertence a outro produto, espelhando o backend (sync.ts). Indice unico no SQLite fica para migracao com saneamento previo. Resolvido 2026-07-01. |
| 60 | Categoria aceita pelo sync diverge do enum validado no web | Baixa | RESOLVIDO | sync usa z.enum(CATEGORIAS).catch("Mercearia"): normaliza categoria fora do enum sem rejeitar o snapshot inteiro (sync/route.ts). Resolvido 2026-07-01. |
| 61 | Basic Auth do admin aceita credencial sem separador dois-pontos | Baixa | RESOLVIDO | middleware exige indexOf(":") != -1 antes de comparar a senha (middleware.ts). Resolvido 2026-07-01. |
| 62 | Campo mobile de alerta de estoque nao limita caracteres na UI | Baixa | RESOLVIDO | TextInput de alertThreshold usa maxLength={FIELD_LIMITS.alertThreshold} (modals.tsx). Resolvido 2026-07-01. |
| 63 | Comentario do schema de rate_limits contradiz a poda ja implementada | Baixa | RESOLVIDO | Comentario reescrito: cardinalidade nao-limitada (IP/token), limpeza e a poda oportunistica em rateLimit.ts (schema.ts). Resolvido 2026-07-01. |
| 64 | Retry de criacao/regeneracao de casa trata qualquer erro como colisao | Baixa | RESOLVIDO | ehColisaoDeCodigo restringe o retry a unique_violation (23505); demais erros propagam de imediato (casa.ts). Resolvido 2026-07-01. |
| 65 | Re-adicao de item excluido e desfeita pelo proximo sync (revive local sem un-delete no protocolo) | Media | RESOLVIDO | LWW com carimbo updated_at em purchase_history: shouldApplyIncomingDeleted no core, usado pelo merge web e applySnapshot mobile; edicoes locais carimbam. +8 testes. Requer db:push (coluna updated_at) e novo build mobile (migracao v8). Resolvido 2026-07-07. |
| 66 | Excluir compra com mais de 24 meses nunca propaga a exclusao | Media | RESOLVIDO | Tombstones de compra ignoram a janela de 24 meses no buildLocalSnapshot (sync.ts mobile). Resolvido 2026-07-07. |
| 67 | Clientes mobile antigos re-inserem como vivas as compras tombstonadas | Baixa | DOCUMENTADO | Codigo atual 1.0.3 e regra da nuvem mitigam; rollout/dispositivos antigos nao verificaveis no repositorio. Revalidado 2026-07-20. |
| 68 | Troca de casa no mobile envia dados locais da conta anterior | Alta | RESOLVIDO (validar em device) | Isolamento por ARQUIVO SQLite por casa (design aprovado pelo proprietario). database.ts virou scope-aware: fileForScope('local')=repona.db, fileForScope(casaId)=repona-casa-<id>.db; setActiveScope() fecha o handle e reinicia migrations no arquivo novo. Servidor passou a retornar casaId (/api/casa e /api/sync). syncClient: pairAndSync agora PUXA (envia SNAPSHOT_VAZIO, recebe casaId+dados, troca de arquivo e aplica na casa) -> dados de outra conta/scratch NUNCA sobem para a casa pareada; criarConta migra o scratch para a conta nova (dado do proprio usuario); active_casa_id no SecureStore; restoreActiveScope() no arranque abre o arquivo certo antes de qualquer leitura; excluirConta apaga o arquivo da casa; unpair/delete recarregam a UI do escopo local. typecheck/lint/build OK. Falta: teste multi-device em runtime (sem harness de sync, #88). Resolvido 2026-07-20. |
| 69 | Pagina publica orienta enviar a unica credencial por e-mail | Alta | RESOLVIDO | Bloco de suporte reescrito: nao pede o token e alerta para nunca envia-lo por e-mail (excluir-conta/page.tsx). Resolvido 2026-07-20. |
| 70 | Action administrativa destrutiva nao revalida autorizacao internamente | Media | RESOLVIDO | excluirCasaAction chama requireAdmin() (revalida Basic Auth por dentro); logica extraida para adminAuth.ts (Edge-safe) usada por middleware+Action. RBAC/MFA/audit-log fora de escopo (app nao tem modelo de identidade). Resolvido 2026-07-20. |
| 71 | Token bearer permanente possui apenas 40 bits de entropia | Media | RESOLVIDO | Sem tokens em producao para migrar (decisao do dono), o comprimento do codigo subiu de 8 -> 12 chars (~40 -> ~60 bits) no mesmo alfabeto sem ambiguos. Centralizado em casa.ts (CASA_CODE_LEN + CASA_CODE_REGEX derivada); atualizados o gerador, todas as regexes (casa.ts, options.ts login, sync route via import) e a UI (login web maxLength/placeholder, mobile PerfilScreen maxLength/placeholder/mensagem, syncClient regex). Mantidas as mitigacoes existentes (rate limit IP+token, cifragem em repouso, fingerprint #43). typecheck/lint/test/build OK. Resolvido 2026-07-20. |
| 72 | Estoque absoluto com LWW perde consumos concorrentes entre devices | Media | ABERTO (aceito) | Fix exige evento idempotente com delta + saldo derivado no servidor (ou CAS por revisao + rebase). Parte do redesenho de identidade/versionamento do sync (com #2, #73); migracoes casadas e teste multi-device. Aceito nesta rodada. 2026-07-20. |
| 73 | Eventos distintos no mesmo segundo colidem na deduplicacao | Media | ABERTO (aceito) | Fix e adicionar sync_id (UUID) estavel por evento de compra/consumo, gerado no cliente, com UNIQUE em Postgres e SQLite e dedupe so por ele. Produtos ja tem syncId; eventos nao. Exige migracoes casadas SQLite(build EAS)+Postgres e compat com clientes que nao enviam o campo. Base do redesenho de identidade do sync. Aceito nesta rodada. 2026-07-20. |
| 74 | Snapshot aceito amplifica recursos e pode ultrapassar lease de 60 s | Media | ABERTO (aceito) | Fix exige limites por custo, lotes menores/bulk, quotas por casa e lock transacional/advisory ou lease renovavel com fencing. Acoplado a paginacao (#55) e idempotencia (#22). Parte do redesenho do protocolo de sync. Aceito nesta rodada. 2026-07-20. |
| 75 | API de sync aceita quantidades invalidas rejeitadas por web/mobile | Media | RESOLVIDO | canonicalQuantity agora exige match integral (^...$: numero + unidade de letras), aplica o teto MAX_QUANTITY_VALUE e positividade contextual via options.allowZero: estoque admite zero, compra/consumo/item de lista exigem positivo (mesma regra das Actions web). "1 ???", "5" sem unidade, valores acima do teto e zero em compra/consumo caem no fallback. Testes no core cobrem os casos. Resolvido 2026-07-20. |
| 76 | Normalizacao Unicode diverge entre SQLite e core de sync | Media | RESOLVIDO | Os 4 lookups do apply de sync (itens de lista, compras, precos, consumos) agora usam productNameKey (NFC) em vez de lower('pt-BR') sem NFC -> corrige o bug de evento descartado em silencio quando o nome tinha acento combinante. Adicionada migration v9: coluna products.name_key = productNameKey(name) + backfill em JS + indice unico guardado; CRUD (create/update) e o match/rename do sync passaram a deduplicar por name_key (Unicode-aware), alinhando o mobile ao lower() Unicode do Postgres do web. typecheck/lint/test OK. Falta so validar em runtime/multi-device (#88). Resolvido 2026-07-20. |
| 77 | Inicializacao/migrations SQLite nao sao serializadas integralmente | Media | RESOLVIDO | initializeDatabase memoiza a inicializacao inteira (roda 1x/sessao, concorrentes compartilham a promise, zera em erro); auto-heal idempotente e best-effort recria o indice unico de nome que a migration v3 pode ter pulado (database.ts). Resolvido 2026-07-20. |
| 78 | Criacao web de produto insere produto e estoque em commits separados | Media | RESOLVIDO | createProduto virou insert unico; ausencia de inventory_items tratada por coalesce e upsert na 1a op de estoque (produtos.ts). Resolvido 2026-07-20. |
| 79 | Builds EAS de desenvolvimento/preview apontam para producao | Media | PARCIAL | API_BASE_URL agora vem de EXPO_PUBLIC_API_BASE_URL por perfil (config.ts/eas.json); dev->emulador, preview->staging. 2026-07-21: removido o fallback silencioso para producao -- se a variavel falta, config.ts LANCA erro explicito em vez de apontar para https://repona.vercel.app (todo perfil do eas.json define a variavel; ausencia = misconfiguracao). Teste config.eas.test.ts garante development/preview != URL de producao. Pendencia operacional inalterada: repona-staging.vercel.app responde 404 -- provisionar backend/banco de staging controlado e NAO distribuir os builds preview atuais. 2026-07-20/21. |
| 80 | Perfil mobile permite criar conta antes de carregar token existente | Media | RESOLVIDO | pairedCode vira tri-state (undefined=carregando/null=nao pareado/string=pareado); enquanto undefined a UI mostra "Carregando" e nao o botao de criar conta (PerfilScreen.tsx). Resolvido 2026-07-20. |
| 81 | Decremento de quantidade fracionaria pode aumentar o valor | Media | RESOLVIDO | changeItemQuantity usa getNextInventoryQuantity+isEmptyQuantity do core (mesma regra do web): "0,8 kg" - 1 mantem o valor em vez de virar "1 kg"; helper local removido. +1 teste no core. Resolvido 2026-07-20. |
| 82 | Falha no bootstrap mobile e exibida como lista vazia | Media | RESOLVIDO | Cada flag ready (products/shopping/history) agora so vira true quando as leituras que alimentam aquela tela tiveram sucesso (productsOk/shoppingOk/historyOk), com update funcional prev||ok para nao regredir tela ja carregada num retry parcial. Uma leitura falha mantem a tela em "Carregando" + alerta de retry, nunca renderiza empty-state como sucesso. Resolvido 2026-07-20. |
| 83 | Tratamento de transporte, timeout e observabilidade e incompleto | Media | RESOLVIDO (obs. residual) | Alem do fetchComTimeout ja existente, o mobile agora VALIDA em runtime a resposta de sync (parseSyncSnapshot em sync.ts: checa estrutura e tipos de products/purchases/consumptions/prices/listItems e casaId numerico) antes de aplicar; resposta malformada vira erro 'SERVER' e nao toca o SQLite, fechando o risco de corrupcao por corpo inesperado. Sem zod (core puro, sem dep nova no mobile). Residual menor de OBSERVABILIDADE (nao integridade): logs estruturados + correlation ID nas Actions/rotas. Resolvido 2026-07-20. |
| 84 | Dados SQLite/fotos podem entrar no backup padrao do Android | Media | RESOLVIDO | O diretorio android/ e gitignored (.gitignore:23) e nao rastreado: builds EAS refazem expo prebuild a partir do app.json, que ja define android.allowBackup=false. O artefato local obsoleto (com allowBackup=true) foi regenerado via `expo prebuild -p android --clean` e o AndroidManifest resultante agora contem android:allowBackup="false" (verificado). Fonte unica de verdade = app.json, sem override nativo commitado. Nota: um gate opcional de CI que falhe se o manifest efetivo nao tiver allowBackup=false segue como hardening recomendado. Resolvido 2026-07-20. |
| 85 | Scripts gravam dumps completos em JSON sem protecao explicita | Media | RESOLVIDO | merge-produtos.mjs passou a criar dir 0700/arquivo 0600 e avisar o operador, mesma protecao de casas.mjs — os dois scripts agora sao consistentes (best-effort no Windows). Criptografia em repouso e retencao/expiracao automatica permanecem como decisao operacional aceita, fora do escopo do script. Resolvido 2026-07-20. |
| 86 | merge-produtos nao preserva/converge todo o estado | Media | PARCIAL | Passa a deduplicar inventory_events (consumos) e a contar/atualizar so compras vivas (deleted=false). Residual: adquirir o lock de sync da casa e criar alias/tombstone old_sync_id->canonical para um mobile offline nao recriar B. 2026-07-20. |
| 87 | Consultas e indices nao acompanham historico/rate limit em crescimento | Media | RESOLVIDO (indices: exec. operacional) | consumoSubquery(casaId) agora junta inventory_events com products e filtra por casaId antes do GROUP BY: o agregado computa so os eventos da casa (produtos.ts). listarHistorico(casaId, limit?) ganhou limite; a pagina /historico pagina server-side por searchParams (TAMANHO_PAGINA=50, busca limite+1 para detectar proxima pagina, link "Carregar mais"), eliminando a busca ilimitada. Indices ja declarados no ORM; aplicacao via db:push no deploy e o unico passo operacional restante (validar com EXPLAIN). Build de producao OK. Resolvido 2026-07-20. |
| 88 | Web e mobile nao possuem testes de fluxos criticos | Media | PARCIAL (harness criado) | Web e mobile agora TEM harness de teste (script test = tsx --test), o gap nomeado: antes so o core tinha. Web cobre agruparHistorico (agrupamento/dedupe de historico) e formatCentsBRL; mobile cobre parsePriceToCents (validacao/teto #29) e formatCentsBRL. Root npm test roda 57 (49 core + 4 web + 4 mobile) e o CI executa. Residual (proxima camada, esforco dedicado): testes de INTEGRACAO — web com Postgres efemero (Actions/rotas/merge/isolamento de tenant) e mobile SQLite/SecureStore (apply/pareamento), que exigem infra de runner. 2026-07-20. |
| 89 | Variaveis criticas nao possuem schema central de ambiente | Media | RESOLVIDO | env-schema.mjs e puro (sem ler process.env) e compartilhado por runtime Next, middleware/admin, Drizzle e CLIs. DATABASE_URL exige postgres/postgresql; AUTH_SECRET/NEXTAUTH_SECRET exigem >=32 e igualdade quando ambos existem; INVITE_TOKEN_SECRET, ADMIN_SECRET e RATE_LIMIT_PEPPER definido usam o mesmo minimo; NEXTAUTH_URL exige HTTPS em producao. env:check valida o conjunto completo sem exibir valores, .env.example/README documentam o contrato e envSchema.test.ts cobre protocolos, aliases, minimos, HTTPS, pepper e erros sanitizados. Resolvido 2026-07-21. |
| 90 | POST/DELETE de conta nao sao idempotentes perante resposta perdida | Media | ABERTO (aceito) | Fix exige Idempotency-Key por operacao persistida no aparelho + resultado memoizado no servidor (UNIQUE operation_id) e tratar 404 pos-delete como conclusao. Parte do redesenho de idempotencia do sync (com #22, #26). Mitigacao parcial via #58 (2026-07-21): salvar o token antes da 1a sync fecha a perda de credencial DEPOIS de recebe-lo; o caso de resposta perdida ANTES do token (casa orfa no servidor) continua exigindo a Idempotency-Key. Aceito nesta rodada; requer planejamento. 2026-07-20. |
| 91 | Criacao de casa aceita disparo cross-origin sem preflight | Baixa | RESOLVIDO | POST /api/casa exige content-type application/json e valida Origin contra NEXTAUTH_URL quando presente; mobile (application/json, sem Origin) nao afetado. Resolvido 2026-07-20. |
| 92 | Controles/modais falham em semantica, foco, alvo de toque e contraste | Baixa | PARCIAL | Passada focada e verificavel: web ganhou role="alert" em TODOS os blocos de erro dinamico (login, inicio, lista, produtos, casa-client) para o leitor de tela anunciar, e aria-label nos botoes so-icone que so tinham title (copiar/gerar token); estrutura semantica (main/header/nav) e aria-label dos controles da lista ja existiam. Mobile: navegacao inferior com accessibilityRole/Label e accessibilityState.selected (FAB "Novo produto" antes sem nome). Residual (passada dedicada com leitor de tela/device): a11y das demais telas/modais mobile, role=dialog/focus-trap nos modais web e auditoria de contraste. 2026-07-21. |
| 93 | Metadados simulados aparecem como dados reais na lista mobile | Baixa | RESOLVIDO | productMeta hardcoded removido; meta derivado de categoria/quantidade reais (shoppingListPresentation.ts). Resolvido 2026-07-20. |
| 94 | Fotos persistentes ficam orfas apos edicao/exclusao | Baixa | RESOLVIDO | handleSaveProduct agora faz rollback do arquivo no catch (apaga a copia nova quando difere da URI de entrada e a escrita falhou). Adicionado collectOrphanPhotos() (products.ts) + listPersistedPhotos() (photos.ts): GC conservador que apaga arquivos do photosDir nao referenciados por nenhum products.photo_uri, rodado uma vez por sessao no startup, idempotente e best-effort. photo_uri so existe na tabela products, entao a comparacao por igualdade de URI e segura. Resolvido 2026-07-20. |
| 95 | Politica de privacidade omite Open Food Facts | Baixa | RESOLVIDO | Secao dedicada divulga a consulta ao Open Food Facts (codigo+IP, sem dados da conta) na politica. Resolvido 2026-07-20. |
| 96 | Pipeline build/CI tem fragilidades de reprodutibilidade e hardening | Baixa | PARCIAL | Feito: Actions pinadas por SHA imutavel (checkout v4.2.2, setup-node v4.4.0), npm ci, job Postgres de baseline (#23), permissions contents:read e -- 2026-07-21 -- gate de audit apertado critical->high apos o #35 zerar os highs. Reclassificado RESOLVIDO->PARCIAL (a analise de status apontou que o escopo original nao estava fechado): residuais ainda abertos = build baixa fontes do Google (next/font -> next/font/local), middleware.ts na convencao depreciada (-> proxy/proxy.ts), overrides manuais de watchFolders/nodeModulesPaths no metro.config.js, ausencia de SAST/CodeQL/secret-scan/dependency-review e referencias moveis de runner/Node major/imagem Postgres. 2026-07-21. |
| 97 | Nomes podem injetar sequencias de controle no terminal dos CLIs | Baixa | RESOLVIDO | casas.mjs e merge-produtos.mjs sanitizam C0/C1/ANSI/bidi (por code point) em todos os campos de dominio impressos. Resolvido 2026-07-20. |
| 98 | Documentacao diverge da arquitetura/configuracao atual | Baixa | RESOLVIDO | apps/mobile/README.md corrigido (telas hoje com dados reais do SQLite; src/ sem "dados mockados"). docs/DATABASE.md ganhou banner de superseded + topologia canonica (Expo/SQLite -> API Next -> Neon) e a linha de Firestore foi corrigida. docs/BACKLOG.md marcou auth/sync Firebase/Firestore como ja implementados via backend proprio. Mencoes remanescentes a Firestore/mock ficam apenas em contexto explicitamente historico (ADR decisions.md com nota de correcao, changelog releases.md datado, item de fase concluida em ROADMAP.md). Resolvido 2026-07-20. 2026-07-21: a analise de status achou mais duas divergencias, corrigidas -- privacidade/page.tsx (token 8->12 chars e "nome escolhido pelo usuario" -> nome padrao "Minha casa", editavel via renomearCasa) e README.md raiz (baseline drizzle/0000_* descrito como retrato reproduzivel do schema, nao "obsoleto", alinhado ao drizzle/README e ao #23). Teste token-consistencia.test.ts cruza CASA_CODE_LEN com o texto publico. |
