// URL base da API do web (Next.js) usada pela sincronização por casa.
// Aponta para o deploy de produção na Vercel. Para testar contra um servidor
// local, troque por http://localhost:3000 (emulador) ou pelo IP da máquina na
// rede (ex.: http://192.168.0.10:3000) em dispositivo físico.
export const API_BASE_URL = "https://repona.vercel.app";
