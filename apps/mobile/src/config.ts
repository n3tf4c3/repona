// URL base da API do web (Next.js) usada pela sincronização por casa.
// Troque pela URL pública do seu deploy (ex.: https://repona.vercel.app).
// O padrão localhost só funciona em emulador na mesma máquina; em dispositivo
// físico use o IP da máquina na rede (ex.: http://192.168.0.10:3000) ou a URL
// da Vercel.
export const API_BASE_URL = "http://localhost:3000";
